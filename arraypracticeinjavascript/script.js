// question no 1
const numbers=[3,7,2,9,5];
let max = numbers[0]; 

for (let i = 1; i < numbers.length; i++) {
    if (numbers[i] > max) {
        max = numbers[i];
    }
}

console.log("maxium number from a array is:",max);
// question no 2
const number=[1,2,3,4,5,6];
let sum=number[0];
for(let i=0;i<=numbers.length;i+=1){
        sum=sum+i;
}
console.log("sum of array is:",sum);

 